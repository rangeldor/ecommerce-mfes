{
  "name": "@ecommerce/orders",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/orders/src",
  "projectType": "application",
  "implicitDependencies": ["@ecommerce/shared"],
  "targets": {
    "dev": {
      "executor": "@nx/vite:dev-server",
      "options": {
        "buildTarget": "@ecommerce/orders:build",
        "port": 3007
      },
      "dependsOn": ["^build"]
    },
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "configFile": "apps/orders/vite.config.ts",
        "outputPath": "dist/apps/orders"
      }
    },
    "preview": {
      "executor": "@nx/vite:preview"
    },
    "test": {
      "executor": "@nx/vite:test"
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "storybook": {
      "executor": "@nx/storybook:storybook",
      "options": {
        "configDir": "apps/orders/.storybook",
        "devServerTarget": "@ecommerce/orders:dev"
      },
      "configurations": {
        "ci": {
          "quiet": true
        }
      }
    },
    "build-storybook": {
      "executor": "@nx/storybook:build",
      "outputs": ["{options.outputDir}"],
      "options": {
        "configDir": "apps/orders/.storybook",
        "outputDir": "dist/storybook/orders"
      }
    }
  },
  "tags": []
}
